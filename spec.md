# MarkdownNT (.mdnt) 格式规范 v0.1

## 1. 简介

MarkdownNT（简称 MDNT）是一种基于 Markdown 的增强型纯文本格式，旨在保持简单、可读、可迁移的同时，原生支持现代笔记场景所需的**音频关联**、**AI 元数据**和**结构化内容**。

**NT 代表：**
- **N**ote —— 为记录而生
- **N**ew Technology —— 融入现代 AI 技术
- **N**ext —— 下一代 Markdown

本规范定义了 `.mdnt` 文件的语法规则和降级显示行为，确保任何文本编辑器都能阅读 MDNT 内容，同时支持高级特性的渲染。

## 2. 文件约定

- **扩展名**：`.mdnt`
- **编码**：UTF-8（无 BOM）
- **换行符**：LF（`\n`）或 CRLF（`\r\n`）
- **MIME 类型**：`text/markdown`（建议，具体可注册 `text/x-mdnt`）

## 3. 标准 Markdown 子集

MDNT 完全兼容以下标准 Markdown 语法（基于 CommonMark）：

- 标题：`#` 至 `######`
- 段落
- 无序列表：`-`、`*`
- 有序列表：`1.`、`2.` 等
- 引用：`>`
- 加粗：`**text**`
- 斜体：`*text*` 或 `_text_`
- 行内代码：`` `code` ``
- 分割线：`---`、`***` 等
- 链接：`[text](url)`
- 图片：`![alt](url)`

> 注意：HTML 块和行内 HTML 暂不支持，以保持格式纯净和安全性。

## 4. 扩展语法

### 4.1 `@meta` 元数据块

用于定义笔记的元信息，如标题、标签、创建时间等。必须位于文件开头（可选）。

**语法：**

```markdown
@meta {
  "key": "value",
  "another_key": 123
}
```

- 花括号内使用 JSON 格式。
- 可以单行或多行。
- 常见字段：`title`、`tags`、`created_at`、`updated_at`、`version`。

**降级显示：** 其他编辑器中将原样显示为普通文本，内容可读。

```markdown
### 4.2 `@audio` 音频块

关联音频文件、时间戳和说话人，用于听记场景。

**语法：**

```markdown
@audio {
  "file": "attachments/audio-20260824.wav",
  "start": 0,
  "end": 12.3,
  "speaker": "张三",
  "transcript": "我们来看一下这个项目的进度"
}
```

- `file`：音频文件相对路径（相对于笔记文件所在目录）。
- `start`、`end`：时间戳（秒），可选。
- `speaker`：说话人标识，可选。
- `transcript`：该音频片段对应的文字内容，可选。

**降级显示：** 在普通 Markdown 编辑器中显示为 `@audio { ... }` 文本块，内容可读。

### 4.3 `@speaker` 说话人标记

标记对话中的说话人，使转写内容更清晰。

**语法：**

```markdown
@speaker 张三
我们来看一下这个项目的进度。
```

**降级显示：** 显示为普通文本行，不影响阅读。

### 4.4 `@ai` AI 内容标记（预留）

用于标记 AI 生成的内容，如摘要、待办列表等。

**语法（示例）：**

```markdown
@ai summary
本次会议讨论了项目进度和下一步计划。

@ai todo
- [ ] 完成需求文档
- [ ] 确认技术方案
```

该标记的具体渲染形式将在后续版本中定义。

### 4.5 `>>>` 代码块

使用 `>>>语言名` 开始代码块，单独一行 `>>>` 结束。

**语法：**

````markdown
>>>python
def hello():
    print("Hello")
>>>
````

- 起始行 `>>>` 后紧跟语言名称（如 `python`、`javascript`、`csharp`）。
- 若语言为空，默认为 `text`。
- 结束行必须单独为 `>>>`（前后允许空格），不允许 `>>>` 后跟其他内容。

**降级显示：** 标准 Markdown 编辑器中，`>>>python` 可能显示为引用或普通段落，代码内容仍然可读。
```

---

### 第 3 段：4.6 节至第 7 节

```markdown
### 4.6 表格

使用标准 Markdown 表格语法，但增加快捷创建提示。

**语法：**

```markdown
| 列1 | 列2 |
| --- | --- |
| 内容A | 内容B |
```

- 表头与内容之间必须有一行由 `|`、`-` 组成的分隔行。
- 列对齐（`:---`、`:---:`、`---:`）支持。

**降级显示：** 本身就是标准 Markdown 表格，在其他编辑器中正常显示。

## 5. 降级显示规则

MDNT 的设计原则是**任何扩展语法在不支持的编辑器中都能以普通文本形式显示，不破坏内容可读性**。

- `@meta`、`@audio`、`@ai` 等块被视为普通段落，按文本原样显示。
- `@speaker` 被视为普通文本行。
- `>>>` 代码块的起始和结束行可能被解释为引用块或普通段落，但代码内容保持原样。
- 所有扩展语法均不依赖 HTML 或隐藏元数据，保证纯文本可见。

## 6. 示例文件

```markdown
@meta {
  "title": "产品评审会",
  "tags": ["会议", "产品"],
  "created_at": "2026-08-24T10:00:00Z"
}

# 产品评审会

@audio {
  "file": "attachments/audio-20260824-100000.wav",
  "start": 0,
  "end": 12.3,
  "speaker": "张三"
}

@speaker 张三
我们来看一下这个项目的进度。

@speaker 李四
我觉得这个方案可行。

@ai summary
本次会议讨论了项目进度和下一步计划。

@ai todo
- [ ] 完成需求文档
- [ ] 确认技术方案

## 技术选型

>>>python
def hello():
    print("Hello")
>>>

| 任务 | 负责人 |
| --- | --- |
| 需求文档 | 张三 |
| 技术方案 | 李四 |
```

## 7. 版本历史

- **v0.1**（2026-08-25）：初版格式规范，定义基础扩展语法。
- **v0.1.12**（2026-08-28）：修复了一些问题，但是保留了无法正常显示斜体、加粗文字的问题，将会修复，并增加了新建、打开、到处HTML等功能。
